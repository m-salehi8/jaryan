# Dependencies for running the Django backend directly on the host (no Docker).
#
# requirements.txt is the full Docker image manifest and also carries the legacy
# FastAPI stack plus several packages that need a compiler (jq, pandas, numpy).
# This file is the subset the Django backend, the process engine and Celery
# actually import, so it installs on a plain machine without build-essential.
#
#   pip install -r requirements-local.txt

# --- Web framework ---
django>=4.2,<5.2
djangorestframework>=3.14.0
django-cors-headers>=4.3.0
django-unfold>=0.30.0
whitenoise>=6.6.0
asgiref>=3.7.0
gunicorn>=21.0.0

# --- Databases ---
# SQLite is built into Python, so no driver is needed. Install psycopg2-binary
# only if you point DB_HOST at a PostgreSQL server.
pymongo==4.6.3
motor==3.3.1

# --- Background jobs ---
celery>=5.3.0
redis>=5.0.0

# --- Auth & config ---
pyjwt>=2.10.1
python-dotenv>=1.0.1

# --- Misc runtime helpers ---
jdatetime>=4.1.1
croniter>=2.0.0
tenacity>=8.3.0
httpx>=0.27.0
tzdata>=2024.2
